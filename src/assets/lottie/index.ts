const lottieFiles = {
    loading: require('./loading.json'),
    notFound: require('./not-found.json'),
    scanner: require('./scanner.json'),
    search: require('./search.json'),
}

export default lottieFiles;